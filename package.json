{
  "name": "gbbo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "setup": "turbo run setup",
    "turbo": "turbo"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "packageManager": "npm@11.6.1"
}
