{
  "name": "gbbo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "setup": "turbo run setup",
    "turbo": "turbo",
    "postinstall": "cd packages/api && poetry install && cd - >/dev/null 2>&1 && cd packages/scraper && poetry install && cd - >/dev/null 2>&1"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "packageManager": "npm@11.6.0"
}
